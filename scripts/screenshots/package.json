{
  "name": "hydra-screenshots",
  "private": true,
  "type": "module",
  "description": "Diff-viewer artifact generator: renders Hydra's web UI to PNGs + a lossless .webm (see ../../.hydra/config.toml [[artifacts]]).",
  "scripts": {
    "screenshot": "bun take-screenshot.ts"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "1.60.0"
  },
  "trustedDependencies": [
    "ffmpeg-static"
  ]
}

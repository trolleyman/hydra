[defaults]
dockerfile_contents = """

# Install unzip (required for bun)
RUN apt-get update && apt-get install -y \\
        unzip \\
    && rm -rf /var/lib/apt/lists/*

# Install Go 1.22
RUN curl -fsSL https://go.dev/dl/go1.22.0.linux-amd64.tar.gz | tar -C /usr/local -xz
ENV PATH=$PATH:/usr/local/go/bin
RUN go version

# Install mage
RUN go install github.com/magefile/mage@latest && mv /root/go/bin/mage /usr/bin/

# Install bun
RUN curl -fsSL https://bun.com/install | bash && mv /root/.bun/bin/bun /usr/bin/bun && ln -s /usr/bin/bun /usr/bin/bunx
"""
pre_prompt = """
You are a head (AI agent) of Hydra, an AI orchestration platform.
- You have unrestricted access to the file system.
- You are allowed to install what is necessary to complete the task.
- You are running inside a Docker container.
- As you work, use git commit to save your progress at logical points.
- Once you have finished the task, make a final git commit with all remaining changes.
- Do *not* use git push.
- Use bun instead of npm, and bun x instead of npx
- Check using mage build before committing

Task:
"""
